import styled from 'styled-components/native'

export const PhotosListContainer = styled.View`
    padding: 15px 0;
    flexDirection: row;
    flexWrap: wrap;
    marginHorizontal: -10px;
`

export const ImageContainer = styled.TouchableOpacity`
    borderWidth: 10px;
    borderColor: transparent;
`